function setup() {
    createCanvas(400, 400);
    background("lime");
  }
  
  function draw() {
  stroke ("black")
    fill("blue");
    
    
    
    if(mouseIsPressed) {
       rect(mouseX, mouseY, 20, 20);
    }
  }function setup() {
    createCanvas(400, 400);
    background("lime");
  }
  
  function draw() {
  stroke ("black")
    fill("blue");
    
    
    
    if(mouseIsPressed) {
       rect(mouseX, mouseY, 20, 20);
    }
  }
    